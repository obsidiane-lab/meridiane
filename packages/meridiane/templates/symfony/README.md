# __BUNDLE_NAME__

Bridge client bundle generated by Meridiane.

## Installation

```bash
composer require __PACKAGE_NAME__
```

## Usage

Cette bibliothèque expose un runtime HTTP minimaliste :
- `BridgeFacade`
- `ResourceRepository`

Les DTOs sont générés dans `src/Model`.
