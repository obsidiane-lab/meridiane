import { Item } from '{{itemImportPath}}';
{{#each imports}}
import { {{this}} } from './{{this}}';
{{/each}}

export interface {{name}} extends Item {
{{#each props}}
  {{{tsKey}}}{{#if optional}}?{{/if}}: {{type}};
{{/each}}
}
